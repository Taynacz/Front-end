# Bibliotech

Projeto criado com Vite + React + Tailwind, tema branco e azul.
Para rodar:
1. `npm install`
2. `npm run dev` (abre em http://localhost:5173)

A pasta `src/legacy/old_project` contém os arquivos do seu projeto antigo (copiados).
